data/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.venv
venv
.coverage
.ruff_cache/
.DS_Store
.env
.env.*
!.env.example

# Private keys and certificates
*.pem
*.key
*.p12
*.pfx

# SQLite databases (brain.db etc, contains private email data)
*.db
*.db-journal
*.db-shm
*.db-wal

# Playwright MCP browser artifacts (runtime, created by chrome/playwright MCP)
.playwright-mcp/

# Superpowers SDD scratch (ledger, briefs, reports, review packages)
.superpowers/

# Coverage artefacts. `.coverage` is a file and `htmlcov/` a directory,
# so both forms are needed; a bare `htmlcov` would not match the directory.
.coverage.*
htmlcov/
